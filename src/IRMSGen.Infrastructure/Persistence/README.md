# Persistence

This folder is reserved for the IRMSGen database implementation:

- `IRMSGenDbContext`
- EF Core entity configurations
- migrations
- audit and generation-run persistence

Development can run migrations automatically. Production should use a controlled CLI or pipeline migration command.
