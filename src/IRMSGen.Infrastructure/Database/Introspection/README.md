# Database Introspection

Schema readers live here. The first production provider should be PostgreSQL, with SQL Server added later.

The reader output should populate metadata for tables, views, functions, procedures, parameters, result shapes, and column mappings.
