# Templates

Generation templates are grouped by output architecture:

- `FeatureBased`
- `CleanArchitecture`
- `MinimalApi`

The generator should render from metadata only. UI and database readers should never write service code directly.
